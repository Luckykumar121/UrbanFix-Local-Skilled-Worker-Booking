services:
  - type: web
    name: urbanfix-backend
    runtime: node
    region: oregon
    plan: free
    rootDir: server
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: MONGODB_URI
        sync: false
      - key: JWT_SECRET
        sync: false
      - key: EMAIL_USER
        sync: false
      - key: EMAIL_PASS
        sync: false
      - key: FRONTEND_URL
        value: https://urbanfix-frontend.onrender.com

  - type: static-site
    name: urbanfix-frontend
    runtime: static
    region: oregon
    plan: free
    rootDir: client/worker-booking-front
    buildCommand: npm install && npm run build
    publishDirectory: dist
    envVars:
      - key: VITE_API_URL
        value: https://urbanfix-backend-production.up.railway.app
